"""CortexClip backend — FastAPI service that turns long videos into
vertical 9:16 clips, ported/improvised from OpenShorts.

Routes live in app/. Run: uvicorn app.main:app --host 0.0.0.0 --port 9000
"""
